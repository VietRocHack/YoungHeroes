# 2026-09-23

## Session 1 — unblock Gemini billing, generate quiz images, fix page-transition direction

Picked up from the 2026-09-21 blocker: Gemini Developer API calls were failing
with `402 RESOURCE_EXHAUSTED` on the CLI-created key.

- User created a new key (`youngheroes-gemini`, later recreated as `Young
  Heroes`) via the AI Studio UI instead of `gcloud services api-keys create`.
  Confirmed via direct `generateContent` calls that this key hits `503`
  ("high demand") instead of `402` — the billing/credit block is gone,
  confirming the CLI-vs-UI key creation gap suspected last session.
- While pushing the new key into Secret Manager, hit a second, unrelated
  blocker: `vietrochack-lab`'s actual GCP Cloud Billing account
  (`011F8E-95E474-1852A1`, "My Billing Account 1") had been closed
  (`open: false`), rejecting the `secrets versions add` call outright. This
  is a different billing surface than the Gemini Developer API credits —
  it's what backs Secret Manager/Cloud Run/Artifact Registry/Hosting.
  User initially reopened a different billing account by mistake ("My
  Billing Account", no "1") — relinked `vietrochack-lab` to that one instead
  via `gcloud billing projects link`.
- Pushed the new key into the `youngheroes-gemini-api-key` Secret Manager
  secret. First attempt (version 3) baked in a trailing `\r\n` from piping
  `gcloud ... get-key-string | gcloud secrets versions add`, which broke it
  as an HTTP header (`requests.exceptions.InvalidHeader`) once deployed —
  fixed by stripping with `tr -d '\r\n'` before writing version 4. Synced
  `backend/.env` for local dev.
- Redeployed `youngheroes-server` on Cloud Run twice (once per secret
  version). Verified the full path live: `/api/new_call` (Firestore) →
  `/api/tts` (Gemini dispatcher + Gemini TTS) returns real `audio/wav` — the
  voice-call feature is confirmed working in production for the first time.
- Ran `backend/scripts/generate_quiz_assets.py` for real — all 23 quiz
  scenario/option images generated successfully into
  `frontend/public/assets/generated/`, matching the filenames already
  referenced in `SkillsRecognize.jsx`/`SkillsCommunicate.jsx`.
- Fixed a page-transition bug: `PageTransition.jsx` always slid new pages in
  from the right and exited to the left regardless of navigation direction,
  so the explicit "Cancel"/"Back" buttons (`navigate(-1)` on
  `PracticeDecision.jsx`/`SkillsCommunicate.jsx`) played the same
  "forward" animation as normal forward navigation. Now direction-aware via
  `useNavigationType()` (`POP` treated as "back", slides the opposite way).

### Infra checklist status (see docs/runbook.md)

Done: everything from before, plus Gemini Developer API calls confirmed
working end-to-end in production, quiz images generated for real.

Not done: custom domain DNS handoff (`youngheroes.vietrochack.com` still
needs its DNS records updated by whoever manages `vietrochack.com` — see
runbook).

### Open TODOs

- Custom domain DNS handoff, still pending on external DNS access.
- No automated tests yet — this session's changes were verified by hitting
  the live Cloud Run URL directly and a manual browser click-through, not a
  test suite (see CLAUDE.md's "No test suite" note).
